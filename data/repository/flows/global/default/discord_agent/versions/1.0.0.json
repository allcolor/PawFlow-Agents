{
  "id": "discord-agent",
  "name": "discord_agent",
  "version": "1.0.0",
  "description": "LLM agent accessible via Discord. Messages from Discord are processed by the agent and responses are sent back.",
  "parameters": {
    "llm_service": "${llm.default.service}",
    "bot_token": "",
    "guild_ids": "",
    "conversation_ttl": 0
  },
  "services": {
    "discord_bot": {
      "type": "discordBot",
      "parameters": {
        "bot_token": "${bot_token}",
        "guild_ids": "${guild_ids}"
      }
    }
  },
  "tasks": {
    "receive": {
      "type": "discordReceiver",
      "parameters": {
        "service_id": "discord_bot"
      }
    },
    "agent": {
      "type": "agentLoop",
      "parameters": {
        "llm_service": "${llm_service}",
        "system_prompt": "You are a helpful AI assistant on Discord. Be concise and helpful.",
        "conversation_attribute": "discord.channel_id",
        "conversation_ttl": "${conversation_ttl}",
        "max_iterations": 50
      }
    },
    "send_reply": {
      "type": "discordSend",
      "parameters": {
        "service_id": "discord_bot",
        "channel_id": "${discord.channel_id}"
      }
    }
  },
  "connections": [
    {
      "source": "receive",
      "target": "agent",
      "relationships": [
        "success"
      ]
    },
    {
      "source": "agent",
      "target": "send_reply",
      "relationships": [
        "success"
      ]
    }
  ],
  "fqn": "default.discord_agent:1.0.0",
  "package": "default"
}