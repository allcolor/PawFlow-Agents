{
  "name": "pawflow-relay-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop manager for standalone PawFlow client relays",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "prepare-runtime": "node scripts/prepare-runtime.js",
    "package:portable": "node scripts/package-portable.js",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check scripts/prepare-runtime.js && node --check scripts/package-portable.js"
  },
  "devDependencies": {
    "electron": "^41.4.0"
  }
}
